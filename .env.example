# Segredos e configurações da aplicação
# Para desenvolvimento, renomeie este arquivo para .env e preencha as variáveis.
# Em produção (como na Vercel), estas variáveis devem ser configuradas no painel do provedor.

SECRET_KEY='uma-chave-secreta-forte-e-dificil-de-adivinhar'

# Para usar SQLite (padrão):
# DATABASE_URL="sqlite:///instance/app.db"

# Para usar PostgreSQL (exemplo):
# DATABASE_URL="postgresql://user:password@host:port/dbname"
